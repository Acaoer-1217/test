spring:
  profiles:
    include: common
  application:
    name: app-admin
server:
  port: 8084
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka
    register-with-eureka: true
    fetch-registry: true