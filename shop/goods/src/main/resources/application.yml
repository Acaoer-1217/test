spring:
  profiles:
    include: common
  application:
    name: app-goods
server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka
    register-with-eureka: true
    fetch-registry: true