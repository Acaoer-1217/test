spring:
  profiles:
    include: common
  application:
    name: app-orders
server:
  port: 8082
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8100/eureka
    register-with-eureka: true
    fetch-registry: true